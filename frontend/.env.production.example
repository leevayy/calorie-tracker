# Copied to `.env.production` for production builds, or set in CI / shell.
# Must match your public API URL (same origin you allow in backend CORS_ALLOWED_ORIGINS).
VITE_API_BASE_URL=https://api.example.com
